# Archived notebooks

These notebooks are not part of the default laptop execution gate.

| Notebook | Why archived | Requirements |
| --- | --- | --- |
| state_change.ipynb | Early Qwen2.5-VL lesion prototype; bypasses the full production scoring path | large model weights and GPU/MPS |
| yeatman2021_state_change.ipynb | Full Yeatman/Qwen perturbation run | public data, large weights, roughly 15 to 40 minutes |
| 09_watch_api_game.ipynb | Paid OpenRouter experiment; prior committed traceback and stale execution order were cleared before archival | API key, network, gymnasium, minigrid, per-step API spend |

The API notebook is retained as an experiment template only. Its narrative,
model ID, environment ID, and current provider behavior must be reconciled and
the notebook rerun cleanly before it can return to the numbered laptop path.
